# OpenRelik EML Worker

## Purpose

Parses `.eml` and `.msg` and extracts headers, body, and attachments.

## Use

- Input: `.eml` or `.msg`
- Output: structured text

## Logs

```bash
vagrant ssh openrelik -c "docker logs openrelik-worker-eml"
```
