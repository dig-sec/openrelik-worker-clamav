# WireGuard Configs

This directory contains Mullvad WireGuard configs used by the firewall VM.

Use the helper script:

```bash
./scripts/wg-config.sh list
./scripts/wg-config.sh select <name>
```

See docs/wireguard/WIREGUARD-SETUP.md for full usage.
