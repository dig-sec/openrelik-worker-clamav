---
# Windows Analysis VM provisioning playbook
- name: Provision Windows Analysis VM
  hosts: windows_analysis
  gather_facts: yes
  vars:
    analyst_password_resolved: "Utgard@Lab2026!"  # Override via --extra-vars if needed
  
  roles:
    - windows-analysis
