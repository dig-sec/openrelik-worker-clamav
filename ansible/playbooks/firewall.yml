---
- name: Deploy firewall/gateway VM
  hosts: all
  become: yes
  
  roles:
    - common      # Base OS setup
    - firewall    # Firewall-specific configuration
    - tailscale   # Join Headscale mesh network
